# TODO — Что осталось доделать

> Задача 2 (обработка реестров платежей/договоров + свод СМСП) — реализована.
> См. `backend/app/services/registry_parser.py`, `smsp_classifier.py`, `primary_sumup.py`,
> `smsp_summary.py`; API `/api/documents/task2/*`; страница `/task2`; вкладка правил
> исключений в админке.

## 🟡 Важное

### 1. Система уведомлений
- [x] Реализовать in-app уведомления (о завершении обработки, ошибках, новых документах)
- [x] Добавить компонент уведомлений в UI (иконка-колокольчик, панель уведомлений)
- [x] Бэкенд: модель `Notification`, эндпоинты для получения/отметки прочитанными

### 2. Экспорт результатов поиска производителей
- [x] Добавить кнопку «Экспорт в XLS/CSV» на странице `ManufacturerSearchPage`
- [x] Бэкенд: эндпоинт генерации XLS/CSV с результатами поиска

### 3. Админ-панель управления пользователями
- [x] Создать страницу `AdminPage` для управления пользователями (список, роли, блокировка)
- [x] Бэкенд: CRUD-эндпоинты для администрирования пользователей
- [x] Добавить маршрут в `App.jsx`, ограничить доступ ролью `admin`

## 🟢 Мелкие доработки

### 4. Dashboard — реальные данные вместо mock
- [x] Заменить захардкоженные `areaData`, `pieData`, `barData` в `DashboardPage.jsx`
- [x] Бэкенд: эндпоинт `/api/analytics/summary` для статистики
- [x] Подтягивать реальные метрики (количество документов по типам, по периодам и т.д.)

### 5. Переключатель сценариев обработки
- [x] Добавить явный выбор сценария (Лидер СМИ / Венета / Д-люкс) на `ProcessingPage`
- [x] Передавать тип сценария в бэкенд, чтобы matcher применял соответствующие правила
- [x] Сейчас логика универсальна и работает, но пользователь не видит, какой сценарий применяется
